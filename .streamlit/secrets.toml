# Streamlit Secrets 範本
#
# 本檔案用於 Streamlit Cloud 部署時設定環境變數
# 請不要提交包含實際憑證的檔案到 Git
#
# 使用方式：
# 1. 在 Streamlit Cloud 的 App Settings > Secrets 中貼上此內容
# 2. 將以下佔位符替換為實際的 Firebase 憑證內容

# Firebase 憑證（JSON 格式）
# 請將整個 Firebase 憑證 JSON 內容貼在這裡
# 注意：保持 JSON 格式的縮排

# 範例：
# [firebase]
# type = "service_account"
# project_id = "your-project-id"
# private_key_id = "your-private-key-id"
# private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# client_email = "your-service-account@your-project.iam.gserviceaccount.com"
# client_id = "your-client-id"
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
# auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
# client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/..."

# Firebase Database URL
FIREBASE_DATABASE_URL = "https://seatlive-status-default-rtdb.firebaseio.com/"
